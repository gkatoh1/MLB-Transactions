services:
  - type: cron
    name: bluejays-transactions-checker
    runtime: python
    plan: free
    schedule: "0 * * * *"  # Run every hour
    buildCommand: pip install -r requirements.txt
    startCommand: python bluejays_transactions.py
    envVars:
      - key: EMAIL_PASSWORD
        sync: false  # This will prompt you to set the value in Render dashboard
